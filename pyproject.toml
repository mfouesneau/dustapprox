[build-system]

requires = ["setuptools",
            "setuptools_scm",
            "wheel",
            "cython",
            "numpy",
            "scipy",
            "pyyaml",
            "pandas",
            "astropy",
            "extension-helpers"]
